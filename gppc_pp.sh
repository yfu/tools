#!/usr/bin/env bash

# Separate a given bed2 file, such that
# a. the first bed2 file contains signals on the plus strand and only records the 5' position of reads
# b. the second bed2 file contains signals on the minus strand and only records the 5' position of reads
# Then this script runs 
